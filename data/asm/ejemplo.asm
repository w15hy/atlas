# Programa ejemplo: suma simple
# ==============================

# mov r0, 10     → R0 = 10
mov r0, 10

# mov r1, 20     → R1 = 20
mov r1, 20

# add r2, r0, r1 → R2 = R0 + R1 = 30
add r1, r0

# halt           → Termina la ejecución
halt
