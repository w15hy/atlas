(opcional) implementar el stack pointer

hacer el dise;o de nuestro computador (de forma grafica)
	- (para mas tardar en 2 semanas) UML para objetos
	- (opcional) como adapatar la memoria cache
	- E / S (interfaz)
	- Definir lo buses (opcional pal que la quiera) fde --> instr --> bus de datos --> bus control 


(yeswah) volver a hacer la codificacion pero que esta vez sea variable
(Yeswah) disco.py | E / O --> Disco Duro --> RAM
(yeswah | opcional) hacer un debugger
(joan)  modos:
		- paso a paso
		- con temporizador (cada tantos segundos)
		- completo


(pa despues) hacer la version grafica

preprocesador - 1 ventana
compilador - 1 ventana
ensamblador - 1 ventana


punto flotante (yeswah) --> dibujo de como funciona o diagrama
vectores,matriz (joan)

lista enlazada (joan)
